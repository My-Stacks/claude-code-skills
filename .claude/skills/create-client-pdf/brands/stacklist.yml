# Stacklist brand
# Currently only the label is consumed by build.py. Future fields (logo path,
# brand-specific accent color, alt typography, etc.) can be added here without
# touching the build script if the template is updated to reference them.

label: "Stacklist"
